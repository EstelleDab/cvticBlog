            <nav class="navbar navbar-expand-lg navbar-light bg-white py-3">
                <div class="container px-5">
                    <a class="navbar-brand" href="{{path('app_home')}}"><span class="fw-bolder text-primary">CVTIC Blog</span></a>
                    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation"><span class="navbar-toggler-icon"></span></button>
                    <div class="collapse navbar-collapse" id="navbarSupportedContent">
                        <ul class="navbar-nav ms-auto mb-2 mb-lg-0 small fw-bolder">
                            <li class="nav-item active">
                <a class="nav-link" href="{{path('app_home')}}">Acceuil <span class="sr-only">(current)</span></a>
            </li>
                            <li class="nav-item"><a class="nav-link" href="{{path('app_front_actualites')}}">Actualités</a></li>
                            <li class="nav-item"><a class="nav-link" href="{{path('app_front_contact')}}">Contact</a></li>
                        </ul>
                    </div>
                </div>

                <div class="navbar">
        <ul class="navbar-nav mr-auto">
            {% if is_granted("ROLE_ADMIN") %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('admin_dashboard')}}">Espace administration</span></a>
                </li>
            {% endif %}
            <li class="nav-item">
                {% if app.user == null %}
                    <a class="nav-link" href="{{ path('app_login')}}">Connexion <span class="sr-only">(current)</span></a>
                {% else %}
                    <a class="nav-link" href="{{ path('app_logout')}}">Déconnexion <span class="sr-only">(current)</span></a>
                {% endif %}
            </li>
        </ul>
    </div>
            </nav>