<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="#">CVTIC Blog</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="{{path('app_home')}}">Home <span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{path('app_front_actualites')}}">Actualités</a>
            </li>

            <li class="nav-item">
                <a class="nav-link disabled" href="{{path('app_front_contact')}}">Contact</a>
            </li>
        </ul>
    </div>

    <div class="navbar">
        <ul class="navbar-nav mr-auto">
            {% if is_granted("ROLE_ADMIN") %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('admin_dashboard')}}">Espace administration</span></a>
                </li>
            {% endif %}
            <li class="nav-item">
                {% if app.user == null %}
                    <a class="nav-link" href="{{ path('app_login')}}">Connexion <span class="sr-only">(current)</span></a>
                {% else %}
                    <a class="nav-link" href="{{ path('app_logout')}}">Déconnexion <span class="sr-only">(current)</span></a>
                {% endif %}
            </li>
        </ul>
    </div>
</nav>